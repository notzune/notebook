the time duration it takes for a [[Signal|signal]] to reach its destination. in [[Computer network|computer networks]], it is the amount of time it takes for the [[Header (computing)|head]] of the signal to travel from the sender to the receiver. it can be computed as the ratio between the link length and the propagation speed over the specific [[Transmission medium|medium]].

it is equal to:

$$
d / s
$$

where 
- $d$ is the distance between [[Node (networking)|nodes]]
- $s$ is the [[Velocity factor|wave propagation speed]]

in wireless networks, $s = c$, i.e. [[Speed of light|speed of light constant]], in [[Twisted pair|copper wire]] (such as [[Ethernet|ethernet]]), the speed $s$ generally ranges from $.59c$ to $.77c$.

***see [[Network delay|network delay]]***